name: Umar PSX Scanner Build

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.22.0'

      # 🟡 اگر آپ کا Flutter project فولڈر کے اندر ہے (مثلاً "app" یا "UmarPSXScanner")
      # تو اس لائن میں وہ فولڈر کا نام لکھیں
      - name: Get Flutter dependencies
        run: |
          cd UmarPsxScanner || cd app || echo "Using root folder"
          flutter pub get

      - name: Build release APK
        run: |
          cd UmarPsxScanner || cd app || echo "Using root folder"
          flutter build apk --release

      - name: Upload release APK
        uses: actions/upload-artifact@v4
        with:
          name: Umar_PSX_Scanner
          path: '**/app-release.apk'
